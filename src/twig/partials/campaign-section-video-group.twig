<div class="container">
    <div class="row">
        {% include 'partials/campaign-section-title.twig' with {
            class: "col col-sm-9 offset-sm-2 offset-lg-0 col-lg-7 mt-7 video-group-border-color",
            text: "Lorem ipsum dolor sit amet, consec tetur adipiscing elit"
        }%}
        <div  class="vertical-video-group">
            {% for item in 1..2 %}
                <div class="video-group--video {% if not loop.last %}mb-4 mb-lg-5 mb-xl-8{% endif %}">
                    {% include 'partials/video.twig' with {
                        id: loop.index,
                        no_transcript: true,
                        hide_caption_text: true
                    } %}
                </div>
            {% endfor %}
        </div>
    </div>
</div>